execute run.sh to experiment with Ruby Machine Learning with Jupyter instance
execute run-python.sh to run Jupyter jupyter/datascience-notebook image

add secret key such as `OPENAI_API_KEY` to `.env` file
